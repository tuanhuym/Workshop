#include<stdio.h>
int sum(int n){
	int sum=0; 
    do{
		int remainder = n%10 ;
      	n = n/10;
      	sum += remainder;
   	}	
	while (n>0);
	return sum;
}
int main(){
	int n;
	printf("nhap vao n:");
	scanf("%d",&n);
	printf("Sum=%d",sum(n));
}
